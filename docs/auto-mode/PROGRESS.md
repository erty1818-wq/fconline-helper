# 완전 자동 모드 — 진행 기록

> 작업을 시작하거나 끝낼 때마다 갱신하고, 코드와 같은 커밋에 넣는다. 계획은 `PLAN.md`.

## 지금 하는 중

- (없음) 남은 것: 실제 게임에서 완전 자동 동작 확인(사용자), AM-04 결과 화면 OCR(결과 화면 캡처를 받으면).

## 상태 표

| ID | 작업 | 상태 | 커밋 | 메모 |
|---|---|---|---|---|
| AM-01 | 인게임 캡처 | 완료 | 20099e8 | Ctrl+Alt+F가 매칭 화면이 아니면 점수판(3배·4배)과 아래 닉네임 줄(3배)을 읽는다. `Core/InGameScreen.cs`, `ScreenReader.ReadRegionAsync`. 실제 스크린샷에서 첫 후보 AUD콜대원(정답), 시각 86 |
| AM-02 | 자동 감시 | 완료(실게임 미확인) | (AM-02~05 커밋) | `App/AutoWatcher.cs` + 규칙 `Core/MatchWatch.cs`(테스트 5개). 게임이 앞에 있을 때만 2초마다 점수판(4배) 읽기, 경기 중이 아니면 두 번에 한 번 전체를 읽어 매칭 화면 찾기. 종료 = 89분 이후 점수판이 12초 사라짐, 또는 60초 사라짐(나감). 실패하면 20초 쉼. **실제 게임에서는 아직 확인 못 함** |
| AM-03 | 상대 카드 (항목 켜기/끄기, 크기 자동) | 완료 | (AM-02~05 커밋) | `MatchCard.xaml(.cs)`, `Services/SquadMoney.cs`. SizeToContent, 폭은 급여·시세·시즌 항목이 켜지면 380, 아니면 320. 구단주 검색 탭 줄 [카드]로 미리 보기. 화면 확인함(Arte정연: 공격 2명이 시세 47%, GK 1%) |
| AM-04 | 경기 종료 카드 | 일부 | (AM-02~05 커밋) | 매칭 카드에 "경기 종료" 띠(마지막으로 본 분, 공식 기록 1~2시간 뒤 안내)를 붙여 띄운다. 점수·기록은 결과 화면 캡처를 받으면 OCR로 추가 |
| AM-05 | 설정 | 완료 | (AM-02~05 커밋) | 완전 자동 선택, 카드 항목 7개 켜기/끄기(`AppSettings.CardSections`). 화면 확인함 |

## 작업 일지

- 2026-09-26 · Claude · 계획 작성. 사용자 인게임 스크린샷으로 OCR 실험(PLAN.md "확인한 사실").
- 2026-09-26 · Claude · AM-01 완료. 테스트 3개(실제 OCR 결과를 고정), 전체 253개 통과. 다음은 상대 카드(AM-03)를 먼저 만들고 자동 감시(AM-02)가 그 카드를 띄운다.
- 2026-09-26 · Claude · AM-02~05. 테스트 10개 추가(MatchWatch 5, SquadMoney 5), 전체 263개 통과. 카드·설정 화면 확인. 완전 자동은 게임이 없어 실제로 돌려 보지 못했다.
- 2026-09-26 · Claude · 캡처 인식: 사용자의 최근 검색에 "ONLINE"이 들어가 있었다(화면의 FC ONLINE을 읽음, 같은 닉 유저가 있어 통과). ONLINE·FCONLINE·EASPORTS·NEXON·FC를 후보에서 뺀다.
- 2026-09-26 · Claude · (사용자 요청) 구단주 검색 첫 화면 아래에 상대 인식 방식 3가지(자동·반자동·수동)를 설명과 함께 늘 보이게 고정. 누르면 바로 적용(`App.SetDetection`), 설정 창과 이름을 맞춤. 함께 고친 것: 개발 빌드가 스스로 릴리스로 업데이트되던 문제(`Updater.IsDevelopmentBuild`), csproj 버전 0.9.0.
- 2026-09-26 · Claude · (사용자 요청) 세 가지.
  1. 스쿼드 OVR을 인게임 수치에 맞춤: `SquadService.WithInGameOvr` = 강화 + 적응도 ⑤ + 팀컬러 + 집중훈련 + 보정 +5 [추정]. 보정은 데이터센터 대표팀 화면 한 개(기린마드리드, 11장)에 맞춘 값이고 원인은 미확인(사용자는 적응도라고 봄, 훈련 코치·클럽 하우스일 수도). 결과: 11장 모두 인게임 수치 ±1. 상대 대표팀 공식 화면은 데이터센터가 Nexon 계정 번호(SN)로만 열어 주고 Open API에는 SN이 없어서 쓰지 못했다.
  2. 약점 카드에 "약점 −25.0" 태그(`PitchView.Tags`).
  3. `Services/ScoutNotes.cs`: 30경기 기록을 평균(캐시한 모든 유저)과 비교한 위험·약점·특징 문장. 요약 탭과 매칭 카드에 표시. `Baseline`에 경기당 득점·가로채기, 슈팅 정확도·결정력·패스·태클 성공률 평균 추가. 슛 type 13·14는 문장에서 뺌. 테스트 2개.
- 2026-09-26 · Claude · 인게임 OVR 검증(사용자 폰 앱 스크린샷, 사디오 마네 +11 적응도 5): 강화 +21·적응도 +4는 맞음(UC 마네 +1 LW 122 → 147 = 폰 147). 등록된 팀컬러도 맞음(리버풀 Lv4 = 전체 +4·속력 +3·반응 속도 +3, 백금빛 물결 Lv2 = 전체 +5). 앞서 모자랐던 약 5는 **상대 스쿼드 계산에 강화 팀컬러가 빠져서**였다. `SquadService.OpponentTargetsAsync` = 감지한 소속·특성 + 강화 팀컬러. 임시로 넣었던 "보정 +5 [추정]"은 뺐다. 기린마드리드 11장 모두 인게임 ±1 그대로.
- 2026-09-26 · Claude · 강화 팀컬러 규칙 수정(사용자 지적, 데이터센터 TeamColorDetail로 확인): 카드마다 자기 강화 단계의 색 하나(백금빛 11강+, 금빛 8강+ …). 전에는 스쿼드에서 가장 좋은 강화 색을 전원에게 줬다(스쿼드 메이커 포함). `TeamColor.AppliesToSquad`는 소속만 true, `SquadBuilder.TeamColorBonus.EnhanceFor`로 카드별 선택. 스쿼드 탭은 선수 능력치를 불러와 세부 팀컬러·집중훈련을 정확히 계산. 기린마드리드 11장: 8장 인게임과 같음, 3장 ±1(실제 집중훈련 능력치와 인게임 포지션을 모름).
