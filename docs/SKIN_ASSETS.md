# 스킨 이미지 교체 가이드 (이미지 생성 AI용)

FC Online Helper의 화면 이미지는 **정해진 이름의 PNG 파일**로 바꿀 수 있다. 다시 빌드할 필요가 없다.

- 넣는 곳: `%LOCALAPPDATA%\FcHelper\skin\` (탐색기 주소창에 그대로 붙여넣기) — 또는 exe 옆 `skin` 폴더
- 파일 이름 = 아래 표의 키 + `.png` (예: `pitch.png`)
- 파일이 없거나 깨져 있으면 기본 그림(코드로 그린 벡터)이 나온다. 바꾼 뒤 앱을 다시 켜면 적용된다.

## 글꼴 바꾸기

`%LOCALAPPDATA%\FcHelper\skin\fonts\` 폴더에 `.ttf`·`.otf` 파일을 넣으면 앱 전체 글꼴이 그 폰트가 된다(예: Pretendard, SUIT — 둘 다 무료 OFL). 폴더가 비어 있으면 설치된 Pretendard → SUIT → Noto Sans KR → 맑은 고딕 순으로 쓴다.

## 공통 스타일 (모든 이미지에 적용)

앱은 어두운 테마다. 이미지가 튀지 않게 다음을 지킨다.

- 배경색 `#15181D`, 패널 `#1E2229`, 선 `#2C323B`, 글자 `#E8EAED`, 보조 글자 `#9AA3AE`
- 강조색은 민트 그린 `#3DDC97` 한 가지만, 보조 강조 `#6CB6FF`(정보)·`#FFB547`(주의)는 아주 적게
- 톤: 차분하고 고급스러운 스포츠 분석 도구. 네온·과한 광택·실제 선수 얼굴·실제 구단 로고·FC온라인/EA/넥슨 로고 사용 금지 (저작권·상표)
- 글자를 이미지에 넣지 않는다 (앱이 한국어로 따로 표시한다)
- 투명 배경이 필요한 것은 PNG 알파 채널 사용

## 이미지 목록

| 키 (파일명) | 크기 (px) | 배경 | 쓰이는 곳 | 설명·프롬프트 예시 |
|---|---|---|---|---|
| `pitch` | 600 × 800 (세로) | 불투명 | 스쿼드 짜기·내 스쿼드의 피치 | 위에서 내려다본 축구장, **위쪽이 공격 방향**. 어두운 녹회색(#17231D 근처) 잔디, 흰 선은 40% 불투명도로 얇게, 약한 잔디 줄무늬, 가장자리 비네팅. 선수 카드가 위에 올라가므로 중앙이 밝거나 복잡하면 안 됨. 경기장 선 위치: 바깥선 여백 20px, 하프라인 y=400, 센터서클 반지름 70, 페널티박스 폭 300·깊이 120 |
| `logo` | 512 × 512 | 투명 | 창 왼쪽 위, 트레이 아이콘 | 5각형 엠블럼, 내부 왕관 및 줄무늬 패턴, 외곽 금색 테크 브래킷 테두리 선. FC 온라인 티어 엠블럼 기반 (고정) |
| `empty` | 160 × 100 | 투명 | 결과가 없을 때 안내 상자 | 미니멀 라인 일러스트: 축구공과 전술판 선, 보조 글자색 선 + 민트 포인트 하나 |
| `player` | 128 × 128 | 투명 | 선수 상세 패널의 기본 이미지 | 얼굴 없는 선수 실루엣(어깨 위), 원형 크롭, 회색 톤. 실제 인물처럼 보이면 안 됨 |
| `hero` | 1600 × 300 | 불투명 | (예약) 상단 배너 | 어두운 경기장 조명 분위기의 추상 배경, 오른쪽이 밝고 왼쪽은 글자가 올라갈 만큼 어둡게 |
## 아이콘 (기본 벡터 + PNG 오버라이드)

앱의 모든 아이콘은 기본적으로 고해상도(100%, 125%, 150% DPI)에서도 선명한 **투톤 벡터(`DrawingImage`)**로 내장되어 있다.
사용자가 `%LOCALAPPDATA%\FcHelper\skin\<키>.png`에 같은 키 이름의 PNG 파일을 넣으면 **PNG 이미지가 벡터 아이콘보다 우선하여 적용**된다. (삭제하면 다시 기본 벡터로 자동 복원)

| 키 (파일명) | 권장 크기 (px) | 배경 | 쓰이는 곳 | 기본 벡터 설명 |
|---|---|---|---|---|
| `nav-squad` | 48 × 48 | 투명 | 메뉴: 스쿼드 짜기 | 전술판 위 선수 점 배치 (선택 시 민트) |
| `nav-picks` | 48 × 48 | 투명 | 메뉴: 숨은 랭커픽 | 별 + 돋보기 |
| `nav-value` | 48 × 48 | 투명 | 메뉴: 가성비 찾기 | 가격표 + 아래 화살표 |
| `nav-grade` | 48 × 48 | 투명 | 메뉴: 강화 효율 | 계단형 막대 + 위 화살표 |
| `nav-salary` | 48 × 48 | 투명 | 메뉴: 급여 효율 | 저울 |
| `nav-trends` | 48 × 48 | 투명 | 메뉴: 시세 추이 | 꺾은선 시세 그래프 |
| `nav-mysquad` | 48 × 48 | 투명 | 메뉴: 내 스쿼드 | 유니폼 셔츠 |
| `nav-opponent` | 48 × 48 | 투명 | 메뉴: 상대 맞춤 | 과녁 + 방패 |
| `nav-teamcolor` | 48 × 48 | 투명 | 메뉴: 팀컬러, 감독모드 팀컬러 탭 | 겹친 원 3개(팔레트) |
| `home-search` | 88 × 88 | 투명 | 홈 화면: 구단주 검색 카드 | 돋보기 + 방패 |
| `home-squad` | 88 × 88 | 투명 | 홈 화면: 스쿼드 도우미 카드 | 전술판 위 선수 점 배치 |
| `home-manager` | 88 × 88 | 투명 | 홈 화면: 감독모드 카드 / 구단주 분석 탭 | 작전판 + 호루라기 |
| `honey` | 32 × 32 | 투명 | 꿀선수 배지 (피치, 목록, 탭) | 호박색 그라데이션 몸통 + 줄무늬 + 반투명 날개 꿀벌 배지 |
| `lock` | 32 × 32 | 투명 | 피치 카드 고정, 버튼 | 자물쇠 |
| `undo` | 32 × 32 | 투명 | 되돌리기 버튼 | 되돌리기 U턴 화살표 |
| `fullscreen` | 32 × 32 | 투명 | 크게 보기 버튼 | 4개 모서리 확장 사각형 |
| `image` | 32 × 32 | 투명 | 이미지 저장 버튼 | 사진 액자 |
| `close` | 32 × 32 | 투명 | 닫기, 삭제 버튼 | ✕ 닫기 |
| `chevron-left` | 32 × 32 | 투명 | 접기 버튼 | ◀ 왼쪽 꺾쇠 |
| `chevron-right` | 32 × 32 | 투명 | 펴기 버튼 | ▶ 오른쪽 꺾쇠 |
| `check` | 32 × 32 | 투명 | 선택된 미니페이스 | ✓ 체크 |
| `update` | 32 × 32 | 투명 | 업데이트 알림, 트레이 메뉴 | 원 안 위쪽 화살표 |
| `warning` | 32 × 32 | 투명 | 주의 알림, 거래 거의 없음 | 노란 삼각 느낌표 |
| `target` | 32 × 32 | 투명 | 상대 구단주 약점 | 타깃 과녁 |
| `note` | 32 × 32 | 투명 | 메모 섹션 | 메모지 |
| `rematch` | 32 × 32 | 투명 | 재대결 전적 | 순환 화살표 |
| `signature` | 32 × 32 | 투명 | 시그니처 골 | 반짝이는 별 |
| `sparkle` | 32 × 32 | 투명 | AI 추천 버튼 | AI 반짝임 |
| `guide` | 32 × 32 | 투명 | 키 발급 방법 보기 버튼 | 펼쳐진 책 |

아이콘 공통: 선 두께 2~3px의 라인 아이콘, 색 `#E8EAED` 중심 + 민트 `#3DDC97` 강조, 가장자리 2~4px 여백. PNG 파일을 넣으면 해당 아이콘에 벡터 대신 그 이미지를 쓴다.

## 이미지 AI에게 줄 한 줄 요약

> 어두운 스포츠 분석 앱(배경 #15181D, 강조 #3DDC97)용 에셋을 만든다. 글자·실존 인물·실제 로고 없이, 미니멀하고 고급스러운 톤. 아래 표의 파일명·크기·투명 여부를 정확히 지킨다.
